# Responsive_Task
A responsive single-page site built with plain HTML and CSS to practice core layout techniques. Uses CSS Grid for page-level section layouts, Flexbox for components like the nav bar and footer, CSS custom properties for theming, and 3 responsive breakpoints. HTML and CSS are kept in separate files with no frameworks or JS.
